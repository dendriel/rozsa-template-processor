data:
  "if-then expression": "result when then value is used"
  "equals symbol operator": "result when then value is used"
  "full if expression": "fallback result from else"
  "not equals operator": "result when then value is used"
  "not equals symbol operator": "result when then value is used"
  "userRole": ""
  "userRole2x": "ADMIN"
  "userRole3": ""
  "userRole4": "ADMIN"
  "userRole5":
  "userRole6": result when then value is used