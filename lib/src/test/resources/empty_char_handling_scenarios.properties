scen01=${	IF 	UPPERCASE(foo) 		==   	bar	 THEN		 result   }
scen02=${   IF  	 UPPERCASE(      foo       ) == bar THEN result}
scen03=${	IF UPPERCASE(	  foo	  ) == bar THEN result}
scen04=${	
		IF
			UPPERCASE(
						foo
					) == bar
  			THEN
  				result
		}